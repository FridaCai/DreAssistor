import './style.less';

var Loading = React.createClass({
  
    render: function() {
        return (
            <span className='loading'>
            </span>
        );
    }
});

module.exports = Loading;
